---
import MainLayout from '@/layouts/MainLayout.astro';
import LegalPage from '@/components/jowoland/shared/LegalPage.astro';
import { termsOfService } from '@/data/legal';
import { createWebPageSchema } from '@/data/site';

const { meta, title, lastUpdated, sections, contact, commitment, intro } =
  termsOfService;

const allSections = [
  {
    title: 'Pendahuluan',
    content: intro.join('\n\n'),
  },
  ...sections,
];
---

<MainLayout
  title={meta.title}
  customDescription={meta.description}
  customOgTitle={meta.title}
  lang="id"
  structuredData={createWebPageSchema('/terms-of-service', meta.title, meta.description)}
>
  <LegalPage
    title={title}
    lastUpdated={lastUpdated}
    sections={allSections}
    contact={contact}
    commitment={commitment}
  />
</MainLayout>
